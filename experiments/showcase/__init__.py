"""showcase — a self-contained entry point with one experiment per category
(recall / efficiency / adaptive / consistency); depends only on `metachase` and the harness
and writes its result under showcase/figures/<category> and showcase/data/<category>.
"""
